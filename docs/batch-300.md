# 300-item batch, measured

| | |
|---|---|
| URL | `https://labelproof.fly.dev` |
| Started | 2026-08-15 16:48:46Z |
| Commit | `1132404` |
| Job | `job_8d916974c5684d78` |
| Manifest | 306 rows — 240 clean, 30 mismatched, 30 unreadable, 6 malformed |
| Upload | manifest 39,346 B, artwork zip 17,411,713 B |
| Accepted | 300 of 306 rows queued |
| Note | first real 300-application run, single-faced artwork, performance-1x, split extraction |

Generated by `scripts/batch_300.py`, which will do this again. **It costs about $6.60 a
run.** One image per application, not two — half the artwork of the 22-item run in
[`cost.md`](cost.md), so the wall clock here is not comparable with that one and is not
presented as if it were.

## The two goals

| Goal | Requirement | Measured | |
|---|---|--:|---|
| 300 applications end to end | PERF-4 / BATCH-2, 10 min | **291.9s** (4.9 min) | **MET** |
| First visible result | PERF-4, 10s | **8.3s** | **MET** |

## Items

| | |
|---|--:|
| Queued | 300 |
| Completed | 300 |
| Failed | 0 |
| Items requeued (attempts > 1) | 0 |
| Total cost | $6.5861 |
| Cost per application | $0.0220 |
| Input tokens | 998,460 |
| Output tokens | 203,865 |
| Cache read tokens | 1,776,060 |
| Cache creation tokens | 0 |
| Throughput | 61.7 applications/min |

Where the $6.59 went: input $2.995, output $3.058, cache reads $0.533. **Output is the
majority of the bill on a batch this size**, which is the same conclusion
[`cost.md`](cost.md) reached per verification and is worth repeating because the intuition
runs the other way — 204k output tokens cost more than 998k input tokens. `cache_creation`
is zero across the whole run: every worker read a prefix somebody else had already
written, so the keep-warm loop paid for the entry and the batch never did.

No item failed.

### Was anything rate-limited?

**Not answerable from failures, so this was re-derived from `attempts`.** The generating
script originally inspected `item["failure"]`, and `api/batch/worker.py` requeues a
retryable provider error up to `MAX_ATTEMPTS` — an item throttled once and served on the
retry carries no failure object at all. The first version of this report printed
"0 rate-limited" about a run in which it could not have seen otherwise.

Re-read from the live job with `GET /batch/{job_id}?limit=1000`:

| attempts | items |
|--:|--:|
| 1 | 300 |

**All 300 items succeeded on their first attempt.** No item was ever requeued, so no
retryable provider error — 429 and 529 included — reached any of them. The claim holds all
the way down because `api/provider/anthropic_adapter.py` builds the SDK client with
`max_retries=0` and says why ("retries are ours"), so the SDK cannot have absorbed a 429
below this counter either.

What this does **not** say: where the ceiling is. Six workers did not touch it, nothing
here locates it, and finding it costs another paid run.

**What the batch recommended**, from the job's own `summary.by_recommendation` over all
300 items:

| Recommendation | Count |
|---|--:|
| ready_to_approve | 240 |
| needs_review | 60 |
| return_for_correction | 0 |
| failed | 0 |

Field verdicts across the 300: 1,590 match, 30 mismatch, 210 unreadable, 270 not
applicable, 0 missing. The 30 mismatches are the 30 rows whose application says 40% where
the artwork says 45%, and the 210 unreadable are the 30 blurred rows at seven fields each
— reported as a statement about the photograph rather than as a finding against the label,
which is the behaviour those rows were put in to check.

> The counts above were re-read from `GET /batch/{job_id}?limit=1000` after the run. The
> generating script asked for the default page of 100 items and printed a mix summing to
> 100 of 300 — worst-first ordering means that page was the 60 needs-review items plus 40
> clean ones, so the table looked plausible and was wrong. `scripts/batch_300.py` now asks
> for the full list and prefers the server's own summary over counting the page it got.

## Row-level validation (TC-20)

**5 rows** were refused by the manifest parser, carrying **24 errors** between them — one
per bad column, which is why the entry count is nearly five times the row count. The
manifest was 306 rows and 300 were queued anyway, because a bad row does not reject the
upload.

**Six malformed rows went up and five came back reported by row number.** The sixth is
entirely blank, and `api/batch/manifest.py:322` skips wholly-empty lines on purpose —
*"a blank line between blocks is not an error"* — so it is dropped silently, and
correctly. It demonstrates that behaviour rather than the row-numbering behaviour. An
earlier version of this file said "24 rows refused ... and the other 300 queued" against a
306-row manifest, which does not add up, and the README claimed all six malformed rows
were reported, which the table below already contradicted.

| Row | Column | Problem |
|--:|---|---|
| 302 | `class_type` | class or type designation is empty. Fill it in and upload the manifest again — this application was not checked. |
| 302 | `commodity` | commodity reads “cider”. It must be spirits, wine or malt. |
| 302 | `net_contents` | net contents is empty. Fill it in and upload the manifest again — this application was not checked. |
| 302 | `producer_address` | producer address is empty. Fill it in and upload the manifest again — this application was not checked. |
| 302 | `producer_name` | producer name is empty. Fill it in and upload the manifest again — this application was not checked. |
| 303 | `alcohol_content` | alcohol content needs a number, for example 45 — write it as a percent by volume. Correct it and upload the manifest again. |
| 303 | `class_type` | class or type designation is empty. Fill it in and upload the manifest again — this application was not checked. |
| 303 | `net_contents` | net contents is empty. Fill it in and upload the manifest again — this application was not checked. |
| 303 | `producer_address` | producer address is empty. Fill it in and upload the manifest again — this application was not checked. |
| 303 | `producer_name` | producer name is empty. Fill it in and upload the manifest again — this application was not checked. |
| 304 | `class_type` | class or type designation is empty. Fill it in and upload the manifest again — this application was not checked. |
| 304 | `net_contents` | net contents is empty. Fill it in and upload the manifest again — this application was not checked. |
| 304 | `producer_address` | producer address is empty. Fill it in and upload the manifest again — this application was not checked. |
| 304 | `producer_name` | producer name is empty. Fill it in and upload the manifest again — this application was not checked. |
| 305 | `class_type` | class or type designation is empty. Fill it in and upload the manifest again — this application was not checked. |
| 305 | `front_image` | No label image is named for this application. Put the image file name in front_image (and back_image if there are two). |
| 305 | `net_contents` | net contents is empty. Fill it in and upload the manifest again — this application was not checked. |
| 305 | `producer_address` | producer address is empty. Fill it in and upload the manifest again — this application was not checked. |
| 305 | `producer_name` | producer name is empty. Fill it in and upload the manifest again — this application was not checked. |
| 306 | `brand_name` | brand name is empty. Fill it in and upload the manifest again — this application was not checked. |

## Verify Now, during the batch (BATCH-9 / PERF-5)

A real `POST /verify` every probe interval while the batch ran. The promise is that a batch does not starve the single-label lane.

| t+ | HTTP | client ms | server ms |
|--:|--:|--:|--:|
| 45s | 200 | 5999 | 5784 |
| 96s | 200 | 6352 | 6140 |
| 147s | 200 | 5854 | 5622 |
| 198s | 200 | 5701 | 5515 |
| 249s | 200 | 5477 | 5305 |

Under load: 5,477-6,352 ms, median **5,854 ms** over 5 probes. Idle control: **one**
request, 5,574 ms.

> **The control is a single request and cannot carry a difference.** The service's own
> warm spread is 5,493-7,031 ms across 8 runs on the same afternoon
> ([`coldstart-probe.txt`](coldstart-probe.txt)), and all five loaded probes sit inside
> it. The honest reading is that **Verify Now under a 300-item batch is indistinguishable
> from Verify Now idle** — not that it is 280 ms slower, which is what the first version
> of this file said by taking `statistics.median` of a one-element list.
>
> Two further caveats. The generating script polls `GET /batch/{id}?limit=1000` every two
> seconds throughout the probe window and not at all during the control, so some of the
> load during the probes is the measurement's own. And what BATCH-9 and PERF-5 actually
> promise is that the batch does not *starve* the single-label lane; five probes that all
> answered 200 well inside the ordinary range support that, and they do not support a
> quantified cost.
>
> `scripts/batch_300.py` now takes three idle verifications as the control by default
> (`--baseline-runs`), so the next run of this can say more than "indistinguishable".

## Progress, as polled

| t+ | done | failed | processing |
|--:|--:|--:|--:|
| 4s | 0 | 0 | 6 |
| 13s | 7 | 0 | 6 |
| 21s | 14 | 0 | 6 |
| 30s | 24 | 0 | 6 |
| 39s | 33 | 0 | 6 |
| 48s | 44 | 0 | 6 |
| 57s | 53 | 0 | 6 |
| 67s | 62 | 0 | 6 |
| 76s | 72 | 0 | 6 |
| 89s | 84 | 0 | 6 |
| 98s | 96 | 0 | 6 |
| 107s | 104 | 0 | 6 |
| 117s | 114 | 0 | 6 |
| 126s | 124 | 0 | 6 |
| 135s | 134 | 0 | 6 |
| 145s | 144 | 0 | 6 |
| 154s | 154 | 0 | 6 |
| 164s | 164 | 0 | 6 |
| 173s | 172 | 0 | 6 |
| 183s | 184 | 0 | 6 |
| 192s | 193 | 0 | 6 |
| 204s | 206 | 0 | 6 |
| 215s | 219 | 0 | 6 |
| 225s | 229 | 0 | 6 |
| 235s | 241 | 0 | 6 |
| 244s | 251 | 0 | 6 |
| 254s | 261 | 0 | 6 |
| 263s | 270 | 0 | 6 |
| 273s | 281 | 0 | 6 |
| 282s | 292 | 0 | 6 |
| 292s | 300 | 0 | 0 |

